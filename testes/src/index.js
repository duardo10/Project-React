import React from 'react';
import ReactDOM from 'react-dom';
import App from './App'; // Supondo que seu componente principal seja chamado App.js

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);
